// Vários parâmetros
const soma = (v1, v2) => {
    return v1 + v2;
}

console.log(soma(10, 5));

// Apenas um parâmetro
const nome = n => {
    return n;
}

console.log(nome('Jardson'));

// Quando não utiliza return
const add = n => n+10

console.log(add(10))